# Carbonizer backend — runtime dependencies.
#
# Production only: backend/Dockerfile installs this file into the runtime image,
# so test tooling does not belong here. Dev/test extras live in
# requirements-dev.txt (or `pip install -e ".[dev]"`, which is what CI uses).
# Pins must stay in sync with [project.dependencies] in pyproject.toml.
fastapi==0.138.0
starlette==1.3.1
uvicorn[standard]==0.32.1
pydantic==2.13.4
pydantic-settings==2.15.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
PyJWT==2.13.0
passlib[argon2]==1.7.4
python-multipart==0.0.31
slowapi==0.1.9
cryptography==50.0.0
prometheus-client==0.25.0
