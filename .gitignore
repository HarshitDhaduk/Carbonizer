# OS / editor
.DS_Store
Thumbs.db
*.swp

# Claude Code (per-user permissions, not for sharing)
.claude/settings.local.json

# Local dev logs from `uvicorn ... > uv.out.log` style runs
uv.out.log
uv.err.log
*.log

# Sub-tree .gitignore files already cover:
#   backend/  → __pycache__, .venv, .env, .pytest_cache
#   frontend/ → node_modules, .next, .env*.local, *.tsbuildinfo, next-env.d.ts
