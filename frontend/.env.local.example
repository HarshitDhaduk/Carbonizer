# Copy to .env.local to point the Next.js client at the FastAPI backend.
# When unset, the app runs on local fixtures (no backend required).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Set to "true" to surface API errors instead of falling back to fixtures.
NEXT_PUBLIC_API_STRICT=false
