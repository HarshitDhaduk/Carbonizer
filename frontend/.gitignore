# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo

# playwright
/test-results/
/playwright-report/
/playwright/.cache/
# next-env.d.ts is intentionally tracked — it has stable, hand-known
# contents (4 lines of /// reference directives), and committing it
# means `tsc --noEmit` works on a cold CI runner before `next lint`
# or `next build` has had a chance to generate it.
/coverage
